#include <bits/stdc++.h>
using namespace std;

int main() {

int n;
cin>>n;
int treated=0;
int untreated=0;
for(int i=0;i<n;i++){
	int scene;
	cin>>scene;
	if(scene>0){
		treated+=scene;
		
	}
	else {
		if(treated<1){
		++untreated;
		}
		else {
			--treated;
		}
	}
}
cout<<untreated;
	return 0;
}
