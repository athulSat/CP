#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,k,j(5),sum(0),count(0);
    cin>>n>>k;
    int total= 240-k;
    for (int i=1; i<=n; i++)
    {
        sum += j* i;
        if (sum > total)break;
        count++;
    }
    cout<<count<<endl;
    
}
